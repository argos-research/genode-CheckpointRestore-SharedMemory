syntax = "proto3";
package protobuf;

import "stored_attached_region_info.proto";

message Stored_region_map_info{
	size_t   const size;
	uint16_t const ds_badge;
	uint16_t sigh_badge;
	repeated Stored_attached_region_info stored_attached_region_infos;
}
